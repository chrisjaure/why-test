import Demo from '../components/Demo'
import TerminalPlayer from '../components/TerminalPlayer'

# Why Test?

Impactful! Improve delevoper experience.

## Saves your team time

- Reduces manual testing.
- Reduces time required to understand code.

## Gives your team confidence

- Changes are less likely to cause regressions.
- Context is encoded and shared.

## Demo

<Demo />

### Proposed change

<CH.Spotlight>
```javascript
<RestrictedContent check={() => user?.age < 18}>
  <Welcome
    name={`${user?.name} 👶`}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age >= 18 && user?.age <= 20}>
  <Welcome
    name={`${user?.name} 🧒`}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age > 20}>
  <WelcomeWithUpdates
    name={`${user?.name} 🧑`}
    updates={user?.updates}
  />
</RestrictedContent>
```

---

Show update count for 18-20 year old users.

```javascript focus=7:9
<RestrictedContent check={() => user?.age < 18}>
  <Welcome
    name={`${user?.name} 👶`}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age >= 18 && user?.age <= 20}>
  <WelcomeWithUpdates
    name={`${user?.name} 🧒`}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age > 20}>
  <WelcomeWithUpdates
    name={`${user?.name} 🧑`}
    updates={user?.updates}
  />
</RestrictedContent>
```
</CH.Spotlight>

### Run the tests!

<TerminalPlayer src="/test.cast" />

### Fix the bug!

<CH.Spotlight>
```javascript focus=7:9
<RestrictedContent check={() => user?.age < 18}>
  <Welcome
    name={`${user?.name} 👶`}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age >= 18 && user?.age <= 20}>
  <WelcomeWithUpdates
    name={`${user?.name} 🧒`}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age > 20}>
  <WelcomeWithUpdates
    name={`${user?.name} 🧑`}
    updates={user?.updates}
  />
</RestrictedContent>
```

---

Spot the bug?

```javascript focus=7:10
<RestrictedContent check={() => user?.age < 18}>
  <Welcome
    name={`${user?.name} 👶`}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age >= 18 && user?.age <= 20}>
  <WelcomeWithUpdates
    name={`${user?.name} 🧒`}
    updates={user?.updates}
  />
</RestrictedContent>
<RestrictedContent check={() => user?.age > 20}>
  <WelcomeWithUpdates
    name={`${user?.name} 🧑`}
    updates={user?.updates}
  />
</RestrictedContent>
```
</CH.Spotlight>

---

[ [Home](/) ] [ [How](/how) ]

